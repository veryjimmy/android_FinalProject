<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.administrator.mytravel">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="旅人趴趴照"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.MainActivity"></activity>
        <activity
            android:name=".ui.ActListActivity"
            android:label="@string/title_activity_act_list"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ui.ActDetailsActivity"
            android:label="@string/title_activity_act_list"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ui.StrategyListActivity"
            android:label="@string/title_activity_act_list"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ui.StrategyDetailsActivity"
            android:label="@string/title_activity_act_list"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ui.LoginActivity"
            android:label="旅人趴趴照">

        </activity>
        <activity
            android:name=".ui.RegisterActivity"
            android:label="旅人趴趴照"></activity>

        <activity android:name=".ui.HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>